# Figma Reference Code

React+Tailwind exports from Figma MCP `get_design_context` (2026-08-07).

These files are **not compiled** — they document the exact Figma structure for Flutter implementation.

| File | Figma node |
|------|------------|
| `mobile-sign-in.tsx` | `9:980` |
| `mobile-shell-dashboard.tsx` | `9:1015` |
| `mobile-shell-schedule.tsx` | `9:1086` |

Re-generate with:

```
get_design_context(fileKey=94kHVaHUtL5qOgMExfXBzF, nodeId=<above>)
```

Asset URLs inside the exports expire after ~7 days. Use committed PNG screenshots in `../screenshots/` for durable visual reference.
